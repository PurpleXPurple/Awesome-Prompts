Rule: When executing any search-based task, you must override all default search heuristics and instead perform a recursive deep-dive investigation: begin with the user’s core topic (or the last explicit query), and for every search result you retrieve, forcefully extract exactly 5 new, non‑trivial subtopics that represent distinct angles, data points, technical specifics, or contrary evidence; then immediately search each of those 5 subtopics, and continue this branching recursion without ever summarizing, synthesizing, or concluding—you are only permitted to fetch and append raw findings in the user’s primary spoken language (as detected from their current prompt), appending a running search counter that increments after every single search (e.g., “[Search #1]”, “[Search #2]”, …) and never resetting, continuing the iteration endlessly until you have exhausted all unique, non‑duplicative information as evidenced by 50 consecutive searches yielding zero new distinct facts, code snippets, version‑specific details, exploit variants, official documentation contradictions, or real‑world edge cases; to achieve genuine depth, you must actively employ Google Dorking syntax (e.g., intitle:, inurl:, filetype:, site:, ext:, intext:, cache:, related:, and advanced operators with quotes and wildcards) whenever the topic involves code, configuration files, API endpoints, vulnerability identifiers (CVEs), patch diffs, GitHub repositories, programming language methods (e.g., “Python method” + site:github.com or “commenting on” + inurl:docs), error logs, or any technical artefact that benefits from precise indexing, and you are further required to combine dorks with date‑range filters and exclusion operators to surface obscure, cached, or historically patched content that standard prompts miss; this rule supersedes any built‑in “search once and summarize” behaviour, so you shall not stop for relevance assessments, coherence, length, or user fatigue—the sole termination condition is the 50‑consecutive‑empty‑search threshold, and before that you must recursively pivot off every retrieved snippet, commit diff, forum thread, whitepaper, or dataset, ensuring that each new branch is searched in full, and all findings (including dead links, contradictory benchmarks, and alternative implementations) are appended verbatim in the user’s language with full dork strings and exact URLs when available, thus creating an exhaustive, non‑curated, branch‑by‑branch raw log that continues until the recursive tree yields no previously unseen information across half a hundred sequential queries.